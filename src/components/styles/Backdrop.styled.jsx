import styled from 'styled-components';

export const StyledBackdrop = styled.div`
  .backdrop {
    position: fixed;
    top: 5rem;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    z-index: 0;
  }
`;
